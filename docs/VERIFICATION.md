# 検証記録

2026-09-19

- Node標準テスト：18回転の逆操作・4回転、入力検証、非可換性。片方向BFSで深さ4まで列挙し、距離2以下の全配置と以降の決定的サンプルを双方向BFSと照合。3〜6手の既知列、探索した各辺の実在、6手上限、二段階探索の解の再現を検証。
- ブラウザ：Codex内蔵ブラウザでBFS→解法の最終頂点→完成を操作確認。内蔵ブラウザの全ページ撮影・表示範囲変更が不安定だったため、Playwright Chromiumで最終検証。
- 320 / 390 / 768 / 1440pxで横スクロールなし。再生・前後ステップ・頂点選択・完成状態・裏の3面・不正入力・中止・20手シャッフルからの解法を確認。ブラウザ例外0件。
- Qwen 3.8 Max / Alibaba / mediumによる独立した数学レビュー。最初の交差点の最短性について反証を要求し、層単位の交互展開の不変条件で正しいと再確認。提案された距離照合チェックを採用。ネイティブエージェントを使用（ブリッジ実行IDなし）。
- npm audit：0件。cubejsの不要な旧npm依存459パッケージを除去。

## 表示の確認

Image Genは環境のUnsupported route / HTTP 404で実行不可。生成コンセプトとの一致は検証できないため、以下の実装基準を使い、デスクトップ・スマホのスクリーンショットを目視した。

| 項目 | 確認と対応 |
|---|---|
| コピー | 日本語の見出し・操作・出典、最短6手と二段階探索の保証範囲を確認 |
| 配置 | デスクトップの2カラム、スマホの縦並び、320pxでも操作ボタンの欠けなし |
| 文字 | Noto Sans JP / IBM Plex Sans、フォームの「適用」の不自然な改行を修正 |
| 配色 | ネイビー背景、開始側シアン・完成側アンバー・解経路の淡青を一貫させた |
| 図 | 現在の54シールと実際の発見木・解の経路からSVGを生成。文字でも色を識別できる |
| 状態 | 解法途中から再探索する際の操作履歴を修正、不正入力表示を次操作で解除 |
| アクセシビリティ | ラベル、フォーカス、ライブ通知、停止、色の文字表記、reduced-motionを確認 |

画像生成コンセプトの代わりに数学図をコードで描画した点は意図的な差異。全グラフの描画や6手を超える最短性は提供しない。二段階探索による解法は提供する。
